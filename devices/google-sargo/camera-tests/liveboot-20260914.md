# Sargo liveboot camera trial — 14 September 2026

Camera image quality is still unaccepted. This trial uses the designated test
phone and a disposable USB-root liveboot; the storage host remains connected.
The selected capture path is **libcamera**. The Megapixels application and its
packaging are out of scope for this PR and are retained only in the local
archived branch.

## Completed

- Booted a coherent `7.1.2-0.pocketfed.sdm670.11.fc46.aarch64` kernel/DTB/module
  bundle containing the LC898219XI lens power-sequence correction. Systemd
  handoff passed with enforcing SELinux, matching modules and an overlay root.
  This establishes boot infrastructure, not camera capture.
- Configured private Wi-Fi and key-authenticated SSH access in the fixture.
  Access credentials, host keys and raw device logs are excluded from this PR.
- Installed stock Fedora libcamera/IPA/tools `0.7.2-4.fc46` for the selected
  capture path.
- Verified the actual evdev Volume Up and Volume Down capabilities; the generic
  readiness gate's 38 host regressions and the libcamera session helper's 17
  process/validation regressions pass.

## First baseline capture (libcamera)

A fresh physical Volume Up gated the `libcamera02` session on the stock Fedora
`0.7.2-4.fc46` stack on the `.11` USB-root:

- 91 completed frames (sequences 0..90).
- The final 1280x960 JPEG fully decoded.
- The strict `power-state.py --require-released` gate passed before and after,
  with no owned remaining processes.
- `zbarimg` decoding of the saved JPEG returned exit 4 with no payload, and the
  file carries no EXIF orientation tag.

This is the first mechanically valid baseline capture. The real log also shows
the requested `RGB888` stream was adjusted to `XRGB8888` by `cam`.

## Visual review (root)

The captured frame is sideways, has a strong cyan cast and is badly
overexposed, losing text; the target was shown on a computer display, not the
printed page. A `zbarimg` decode of the saved frame returned exit 4 with no QR
payload, and the JPEG carries no EXIF orientation tag to correct its sideways
pixels. Image quality is therefore
**not accepted**. This is a baseline for a mechanism/quality gap, not a usable
photograph.

## Camera enumeration issue

CCI initially failed probing with `deferred probe timeout, ignoring dependency`
and error `-110`. CAMSS existed, but neither sensor nor the lens was registered.
After all CCI suppliers were bound and available, one explicit bind of the
unbound CCI controller succeeded. IMX363, IMX355 and LC898219XI then appeared,
and the strict power check reported all three suspended with no camera handles.

This was a recovery in the running trial; the initial boot did **not** pass
camera enumeration. The `.11` bundle already carries the CAMCC provider for the
CCI power domain (`CONFIG_SDM_CAMCC_845=m`, DTB compatible `qcom,sdm845-camcc`),
but CAMCC registered after the deferred-probe timeout. A one-line initrd
early-module correction is prepared in the liveboot harness profile
(`camcc_sdm845`) as draft PR #67 stacked on #61, with no command-line timeout
change. It remains **boot unvalidated**: a subsequent boot must confirm that
CCI, both sensors and the lens enumerate on their own.

## Historical Megapixels evidence

Before the user selected libcamera, a Megapixels 2 trial reached real rear
preview and saved three DNGs at 4032x3024, but the application crashed in its
postprocessing step before writing any JPEG; the strict camera release check
passed afterwards. That path is out of scope and its code is removed from this
PR (retained only in the local archived branch). Those DNGs are not a
usable-JPEG acceptance result.

## Native libcamera candidate

The phone build used the pinned upstream 0.7.2 clone and matching Fedora source RPM.
The native helper verified the source hashes, clone identity, presence of the
Fedora IPA re-signing command and subpackage layout on aarch64. The signing
command's presence did not establish valid signatures. A two-job unprivileged build of
`4.fc46.native.1` then **finished** on the phone, applying only the pinned
postmarketOS IMX363 tuning task patch (`0001`). It produced 16 binary and debug
RPMs plus a source RPM, backed up privately, and an independent SHA-256 check of
that backup matched the recorded hashes. A specifically selected set of 20
non-camera libcamera library tests passed with no failures.

Two facts limit this candidate. All five IPA modules in the built artifacts
**failed signature verification against the build public key**, because the
Fedora spec's post-strip re-sign step uses the obsolete glob
`%{_libdir}/libcamera/ipa_*.so` while 0.7.2 installs the modules under
`%{_libdir}/libcamera/ipa/`; that glob was confirmed to match nothing in the
build output. The current repository helper rewrites the glob for a future
build and its default patch directory also carries the optional, still-unbuilt
`qcam` bounded-save patch `0002`.

`4.fc46.native.1` is **not installed**, and the installed stack remains stock
Fedora `0.7.2-4.fc46`. No capture and no image quality is claimed for the
native candidate.

## Trial interrupted by host reboot

The host rebooted around 15:48. That discarded the host's `/tmp` worktrees and
interrupted USB-root hosting. The camera worktree was restored at `c2e4b0c0`,
and the JPEG, logs and RPM archive remain available on persistent host storage.
The compiled objects were retained in the phone's `/run`; that RAM state cannot
be recovered until device access returns and will be lost if the phone reboots.
The phone still advertises its USB-root interface but does not answer SSH.
No capture test is active. The prepared CAMCC boot's input hashes were rechecked;
its hardware trial awaits the phone returning to ABL fastboot.

## Pending

On-screen UI remains unverified, and the one saved frame did not decode to the
expected QR payload (see the visual review above). Manual focus, plausible
colour, correct orientation, printed-page readability, a useful saved JPEG,
repeated capture, reopen, reboot and release-after-capture remain to be
accepted on the libcamera path. Suspend/resume cannot be accepted from this
USB-root baseline while the default sleep masks are retained.
