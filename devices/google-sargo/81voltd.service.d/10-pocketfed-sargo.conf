[Unit]
# Upstream v1.2.0 orders itself after a non-Fedora ModemManager.target.
Wants=ModemManager.service
After=ModemManager.service
# Keep retrying if the system bus or QRTR is not ready during early boot.
StartLimitIntervalSec=0

[Service]
Restart=on-failure
RestartSec=5s
