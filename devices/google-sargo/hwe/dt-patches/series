# Devicetree patches applied, in order, to the DTS of the kernel-ark tag that
# built the pinned Fedora kernel (tools/hwe/patch-dtb.py). Each file is kept
# exactly as it is (or will be) posted upstream; the build refuses fuzz and
# reports a patch that has landed upstream so it can be dropped here.

# "Add support and enable the debug UART in the Pixel 3a and Pixel 3a XL",
# v5 (Pablo Correa Gómez), prepared 2026-09-25 against v7.3-rc4, not yet
# posted. Replace with the lore copy once it is sent.
arm64-dts-qcom-sdm670-add-default-uart-pinctrl-nodes.patch
arm64-dts-qcom-sdm670-add-debug-uart-soc-node.patch
arm64-dts-qcom-sdm670-google-common-enable-debug-uar.patch
