# The bring-up GLINK quarantine blacklists qcom_glink and qcom_glink_smem on the
# kernel command line. qcom_q6v5_mss depends on both, so autoloading it would
# only log a refused insert on every boot; keep it deferred to blob-wrangler,
# whose modprobe helper skips it.
blacklist qcom_q6v5_mss
