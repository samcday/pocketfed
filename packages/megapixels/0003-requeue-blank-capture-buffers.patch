--- a/src/io_pipeline.c
+++ b/src/io_pipeline.c
@@ -407,6 +407,7 @@
 
                         if (image_is_blank) {
                                 ++state_io.blank_frame_count;
+                                mp_io_pipeline_release_buffer(buffer.index);
                                 return;
                         }
                 } else {
