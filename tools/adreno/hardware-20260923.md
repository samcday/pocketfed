# A3xx hardware controls, 2026-09-23

Follow-up to [Rob Clark's requests](https://gitlab.freedesktop.org/mesa/mesa/-/work_items/12634#note_3674373)
and the [host evidence audit](rob-followup.md). This is an ongoing experiment,
not a production Mesa fix. New results below use the prepared Fedora 46
Mesa 26.2.2 libraries on DB410c/A306 with Fedora 7.3-rc3. B1–B8 and B10 use its unchanged MSM module; B9 tests the
separately identified recovery candidate. B11 uses the mask-only correction,
B12 combines recovery and mask corrections, and B13 adds the initialization guard.

## First boot: minimal clear + draw

All four runs use the same reduced trace and display session, with shader cache
disabled. The guest verified every payload checksum. Both `ldd -r` and the
running retracer's maps identify the intended override; captured environment
confirms the chosen `FD_MESA_DEBUG`. Scanout was active before the trials.

| Trial | Library | Mode | GPU result (retired/submitted fences) | GPU clock reference counts |
| --- | --- | --- | --- | --- |
| t01 | stock | sysmem,flush | No hang; fences 943/943 | unchanged |
| t02 | direct-wait | sysmem | No hang; fences 961/961 | unchanged |
| t03 | direct-no-wait | sysmem | No hang; fences 979/979 | unchanged |
| t04 | wait-only | sysmem | Hang; fences 991/994, then recovery | all six GPU-owned clocks +1 |

The direct arms contain no per-upload logging. Neither passing arm caused a
hangcheck; final RBBM status was `0x1`, ring pointers matched, and the same
compositor remained alive. These are replay/completion checks, not pixel or
performance claims. Reported frame timings include compilation and setup.

Wait-only still returned process exit 0 after recovery; it is **not a pass**.
At uptime 353.776 s the kernel detected a lockup, recovered at 353.798 s, and
reported a ring-drain timeout at 354.870 s with RPTR 0 / WPTR `0x1726`.
The compositor then crashed with SIGSEGV. Final fences 994/994 establish
retirement after recovery, not an intact rendering session.

This gives a narrow answer to the CPU-prep question: adding B2's prep call to
the unchanged indirect path was insufficient here, while the matched direct
path worked without that explicit call. Mapping/copying, command layout,
source visibility and timing remain distinct possible explanations.
The matched wait arms deliberately preserve B2's ignored prep return; these
runs establish behavior with that call present, not independently verified
successful prep. The error-checked follow-up also hangs without a reported
prep error; see B8.

The first new hardware dump is 94,243 bytes, SHA-256
`eeff1f07a9c91d5d67d65ce146a11eeeeb0c954f7dce9d487fedf5d7b222b69c`.
The host copy matches the guest hash. It reports RBBM `0xe0684003` and submitted/retired fences
994/991. Command BO `0x0348e000` still contains the expected indirect loads:

```text
+0x488: VS DST_OFF=16 NUM_UNIT=16, source=0x03a99240 (128 bytes)
+0x558: FS DST_OFF=16 NUM_UNIT=32, source=0x03a99240 (256 bytes)
```

All ten CP_LOAD_STATE packets decode without warnings. Raw logs and dumps stay
in ignored local artifacts.

## Second boot: full trace, pixels, and stock negative control

An orderly reboot restored baseline clock counts and a new display session.
Trials t05–t10 use the full `r3.trace`, disabled shader cache, and the same
verified library overrides and environment. All completed before any B2
hang/recovery; phoc PID 1109 and active scanout remained intact throughout them.

| Trial | Library | Mode | Replay | GPU result (retired/submitted fences) | GPU clock references |
| --- | --- | --- | --- | --- | --- |
| t05 | stock | sysmem,flush | Full trace, no readback | No hang; 993/993 | unchanged |
| t06 | direct-no-wait | sysmem | Full trace, no readback | No hang; 1011/1011 | unchanged |
| t07 | direct-wait | sysmem | Full trace, no readback | No hang; 1028/1028 | unchanged |
| t08 | stock | sysmem,flush | Full trace, PNG at call 6260 | No hang; 1061/1061 | unchanged |
| t09 | direct-no-wait | sysmem | Full trace, PNG at call 6260 | No hang; 1079/1079 | unchanged |
| t10 | direct-wait | sysmem | Full trace, PNG at call 6260 | No hang; 1098/1098 | unchanged |
| t11 | stock | sysmem | Minimal clear + draw | Hang; 1112/1115, then recovery | all six GPU-owned clocks +1 |

The three PNGs have identical decoded pixels and identical SHA-256:
`2d34c84ae83f9d0814880c0d062bf664572f191a93e6cb6beb02889ce8e36775`.
Host hashes match the guest hashes. All are 1280×688 with 653 RGB colors;
visual inspection shows the actual GTK gradient probe and its text, rather
than an empty frame. The local `hardware/pixels/comparison.json` records the
comparison. These establish equality to the stock+flush reference at this
captured frame, not general rendering correctness. Separate no-readback
trials t05–t07 establish that adding the snapshot was not required for those
completion results.

The stock indirect negative control t11 used the minimal trace without
`flush`. It hung at uptime 711.728 s, recovered at 711.750 s, and reported the
same post-reset drain failure at 712.822 s, this time RPTR 0 / WPTR `0x1EBC`.
It returned exit 0 and eventually retired fence 1115, but phoc PID 1109 crashed
with SIGSEGV at 713.687 s. The AFTER display check showed active scanout and a
replacement phoc PID 3213: this was a crash/restart, not an intact session.

Together with the first boot, this extends the direct-no-wait result to the
full trace and matching captured pixels. The added explicit CPU-prep call is
not required for those observed direct-upload passes, and adding it to the
indirect path did not prevent the minimal-trace hang. These CPU-initialized
trace results still leave source visibility, copying, packet layout and timing
as possible explanations; they do not establish behavior for GPU-produced
constant data.

## Third boot: fresh-source indirect

Trial t12 used `fresh-source + sysmem` on the same minimal trace, from a new
boot with baseline clocks. The library hash, runtime maps and environment
match the prepared control; no `FD3FRESH` fallback was logged. It hung at
162.738 s with retired/submitted fences 1907/1908, recovered at 162.760 s,
then timed out draining RPTR/WPTR `0/180E` at 163.826 s. All six GPU clock
enable/prepare counts increased by one. phoc PID 1059 crashed with SIGSEGV at
165.950 s and was absent from the final process check. Exit 0 is again not a
passing result.

The 95,883-byte devcoredump has SHA-256
`ccb5bcd3f62f84b96d495854ad7ff0275529f19ef7248ddca80cfa32f4ddda73`.
It reports the same RBBM `0xe0684003`, with the two indirect loads unchanged
in size and source offset: VS 128 bytes from `0x02cd4240`, FS 256 bytes from
`0x02cd8240`. These reference distinct 16-KiB source BOs at `0x02cd4000` and
`0x02cd8000`. Their captured contents, reconstructed with the zero tails
trimmed by the kernel dump writer, are byte-identical. The original UBO lacks
captured data, so this dump cannot independently prove original-to-copy
equality or what the GPU saw through its caches. The captured 13,792-byte FS
again matches the host p7/B10 shader byte-for-byte.

The saved ring has RPTR=WPTR=6158, unlike the earlier stock/wait snapshots;
this is a hang with fresh captured sources, not proof of an identical stall
point. The later printed fence equality is the recovery worker's software
fence advance, not evidence of intervening GPU progress.

## Assertion-enabled scheduler comparison: B5 and B6

After the B4 boot failure, a physical reset and new root-server process allowed
both trials to boot normally. Each used the same eight-arm root with all eleven
payload checksums verified, the stock loaded module, active scanout and a healthy
compositor before replay. The O2/assertions-on pair differs only by the two
!44620 commits. Runtime maps, environment and loader checks identify the intended
library; shader cache was disabled and both used the minimal trace in sysmem.

| Boot / trial | Library | GPU result (retired/submitted) | Assertion | Compositor |
| --- | --- | --- | --- | --- |
| B5 / t13 | stock-asserts | Hang at 223.729 s; 2007/2010 | No failure | phoc 1065 SIGSEGV at 226.111 s |
| B6 / t14 | sched | Hang at 389.743 s; 1220/1223 | No failure | phoc 1055 SIGSEGV at 392.226 s |

Both retracers returned exit 0 after recovery, and both increased all six GPU
clock enable/prepare counts by one. The MR therefore does not fix this GTK
minimal reproducer. Both captured fragment shaders are byte-identical to each
other and to the earlier host/hardware shader: 13,792 bytes, SHA-256
`6b277e86d377c9497a3d6c9c8fc55fa9300627c937d42b8ef68907f12f19d788`.
The unchanged VS/FS indirect loads request 128/256 bytes from source offset 576.
This shader has no kill/discard/demote and does not exercise the specific
cross-block kill/bary.f condition; these results do not evaluate that condition
or SuperTuxKart shaders.

Guest-hash-verified devcoredumps:

- t13: 94,277 bytes, `62e36db7c55291ae29a5737da34e7537d762ba6cf6100675846c1de70e91b2e1`.
- t14: 94,276 bytes, `c6f9b6d53427e8971981b510322bab864eb27571115ed8ebec50db14b89dd8e8`.

Both runs used the scoped return-tracing helper. The recovery GPU-worker's
`a3xx_pm_suspend()` returned `-16` (`-EBUSY`) to `adreno_recover+0x34`, followed
by `msm_gpu_pm_resume()` returning 0 to `adreno_recover+0x44`. Generic suspend
was never called. There were zero missed probes and zero per-CPU buffer
overruns or dropped events. Other `-EBUSY` returns came from normal runtime
suspend; the recovery pair is distinguished by its caller/task. The return
sequence directly confirms the previously inferred recovery mechanism.

## Recovery clock observation

Trials t04, t11, t12, t13, t14 and t15 reproduced
the same change from their own fresh-boot baseline. Before and after each trial, GPU runtime status
was active with `power/control=auto` and runtime suspended time zero. The
following enable **and** prepare counts changed together after each recovery:

| Clock | Before | After |
| --- | ---: | ---: |
| gcc_oxili_gfx3d_clk | 1 | 2 |
| gcc_oxili_gmem_clk | 1 | 2 |
| gcc_oxili_ahb_clk | 1 | 2 |
| gcc_bimc_gfx_clk | 1 | 2 |
| gcc_bimc_gpu_clk | 1 | 2 |
| gfx3d_clk_src | 3 | 4 |

The IOMMU clocks and `bimc_gpu_clk_src` parent counts did not change; rates stayed
unchanged. All six GPU references were stable through the non-hanging B2
controls t05–t10. The B5/B6/B8 callback traces directly confirm the
[failed-suspend/unconditional-resume mechanism](recovery-clocks.md) identified
in source and Fedora's shipped module. This is evidence about recovery, not the
cause of the original hang, and runtime suspended time does not count direct recovery callbacks.

The separate [draft kernel correction](https://github.com/samcday/linux/pull/5)
uses generic suspend during A3xx recovery after software reset, preserving
normal runtime-suspend idle/VBIF checks and the resume/hardware-init sequence.
Apply/whitespace/checkpatch checks pass. The three affected ARM64 translation
units (`adreno_gpu.o`, `a3xx_gpu.o`, `msm_gpu.o`) also compile against the exact
upstream base with upstream arm64 defconfig; logs/config/compiler provenance
are archived under `out/adreno-rob-20260923/recovery-evidence/kernel-cross-compile/`.
A separate complete `msm.ko` build now passes with the exact shipped Fedora
configuration, generated headers and symbol table, using its matching ARM64
compiler/linker. Its vermagic, 784 imports and ten dependencies match stock;
the Fedora source RPM comparison and emitted recovery branch were checked.
The CRC32-XZ candidate has SHA-256
`f128ed08e25e826e6c714c1a3aa2e718d1450f3e63356d05c4bc8cfe0ab7bfe1` and build ID
`8781436f3c1601233e20bdb11e67be06244fe222`. Evidence is under
`out/adreno-rob-20260923/recovery-evidence/fedora-module/`. This unsigned
external module lacks module BTF; static compatibility checks do not establish
loadability or recovery behavior. B9 below now verifies the loaded candidate
and its first recovery; broader coverage is recorded separately.

B4 failed during boot with ext4 inode-checksum errors before `/sbin/init`; no scheduler trial
started. The served root's SHA-256 still matched the baked image. This is a
fixture/boot failure, not a scheduler result. Subsequent B5/B6 boots passed those
checks. Further comparisons still require a verified fresh session after a hang
changes clock references and compositor state.

## B7: login failure before checked-wait trial

The next stock-kernel boot reached automatic root login but never produced a
shell prompt. No checked-wait replay started, and no GPU hangcheck was recorded.
The root-server process had been restarted before this boot. The served image
still has its baked SHA-256
`006e8196cf81a419c51d9e52e32a9edaa38b06edb8a5eedfa68841161a660114`.
Serial interrupts did not recover a prompt; a physical reset was requested.
This is a boot/login failure of unknown cause, not a Mesa diagnostic result.
The subsequent B8 retry completes checked-wait below; B9 then tests the module.

## B8: checked-wait also hangs without a reported prep failure

A physical reset and fresh root-server process restored a healthy boot. The
stock loaded-module note and all eleven payload checksums matched; phoc PID
1029 had active scanout. `ldd -r`, runtime maps, library hash and environment
identify the matched release/O3 `wait-checked` arm in sysmem, with cache disabled.

Trial t15 hung at 221.744 s with retired/submitted fences 1875/1878. It emitted
no `FD3WAIT_CHECK` preparation error and did not abort: the process returned 0
after recovery. This diagnostic propagates existing fence-wait errors and
aborts on a nonzero prep result before the unchanged indirect emission. Thus
this run does not hide a reported preparation failure. Zero still permits an
already-idle fast path; it does not prove that a blocking wait occurred.

Recovery at 221.766 s was followed by the drain timeout at 222.838 s, RPTR/WPTR
`0/15EA`, and phoc SIGSEGV at 223.884 s. All six GPU clock enable/prepare counts
increased by one. The `gfx3d_clk_src` AFTER line was interrupted by an audit
message after its counts/rate; a separate compact read confirmed 4/4 and the
unchanged rate. Neighboring comparison-clock counts/rates remained unchanged.
The scoped trace again records recovery suspend `-EBUSY`, resume 0, no generic
suspend, zero missed probes and zero per-CPU buffer loss. Scoped cleanup passed.

The guest-hash-verified dump is 94,277 bytes, SHA-256
`e4223653e4b868704c24be2742cbab10aef2125c58bf570f274d771a663232c3`.
It reports RBBM `0xe0684003`, saved RPTR/WPTR 5600/5610, and unchanged VS/FS
indirect loads of 128/256 bytes from UBO+576. The 13,792-byte FS is byte-identical
to the stock t11 shader. The original source UBO has no captured payload.

## B9: candidate recovery module and rendering after recovery

The temporary boot preserves the same root image, kernel Image and DTB and
changes only the initramfs MSM module. Its live module-note SHA-256 is
`26a44dfeccef1cc8b948b6a68983334760c61831783faae4a907aa37e8cad56b`, matching the
built candidate. Fedora accepts the unsigned external module with taint 12288.
All eleven payload hashes, loader checks, active scanout and phoc PID 1083 were
verified before the first trial.

Stock/sysmem minimal trial t16 still hangs at 239.727 s (retired/submitted
1456/1459), with recovery at 239.750 s. The candidate directly calls generic
suspend and resume, both returning 0 to `adreno_recover`; its A3xx suspend
callback is not called by recovery, and no stale-ring drain timeout appears.
All six GPU clock enable/prepare counts remain at their fresh-boot baselines.
The scoped capture has zero missed probes and zero per-CPU buffer losses;
cleanup succeeds. Normal runtime-suspend failures in the same capture have
distinct `adreno_runtime_suspend` callers.

The Mesa hang and compositor failure remain: phoc 1083 crashes with SIGSEGV
at 241.166 s. The display-manager alias resolves to `phrog.service`, which
automatically starts a replacement phoc PID 2674. This is recovery of the
service, not survival of the original display session.

After confirming that replacement's active scanout, t17 replays full R3 with
stock Mesa and `sysmem,flush`. It completes with fences 2170/2170, unchanged
clocks and the same replacement compositor. Its 1280×688 PNG at call 6260 is
byte-identical to the preexisting t08 reference (SHA-256
`2d34c84ae83f9d0814880c0d062bf664572f191a93e6cb6beb02889ce8e36775`). This
establishes rendering after recovery/service restart at that captured frame.

The t16 devcoredump is 94,276 bytes, SHA-256
`5f9ff65e5fcfd0b7585882087875c11ab5347a0ca2815490bc6b538893294914`.
It retains RBBM `0xe0684003`, RPTR/WPTR 6246/6260, and VS/FS indirect loads of
128/256 bytes from UBO+576. The dump, return-trace directory and t17 PNG were
transferred and verified against guest hashes before further trials.

A second deliberate stock/sysmem minimal hang, t18, occurs at 491.759 s with
fences 2185/2188, recovery at 491.782 s and phoc 2674 SIGSEGV at 492.474 s.
Both generic recovery calls again return 0, there is no stale-ring timeout,
and all six clock counts remain balanced. This capture also has zero missed
probes/buffer losses and successful scoped cleanup. The previous devcoredump
was hash-checked and acknowledged before t18, so the new dump is distinct:
94,256 bytes, SHA-256
`5b8ba94d6b1f10693b6b0bcd4844b7c19b4d600919452badba4369405c042898`.
Both candidate-run fragment shaders match each other and the prior stock FS.

The subsequent ordinary runtime-PM check t19 stops `phrog.service` and confirms
an empty DRM client list and no remaining phoc/greetd process. Normal A3xx
suspend returns `-EBUSY`; runtime status stays active, suspended time stays
zero, and no generic suspend/resume occurs. The display service is restored
successfully. This is not a successful idle suspend/resume cycle. It follows
two recoveries; B10 below independently reproduces the idle failure on a clean
stock boot and localizes it to the VBIF mask. All t19 probes and buffers were captured without
misses/loss and cleaned up.

## B10: clean stock idle failure and A306 halt-mask mismatch

A fresh stock-module boot, with no preceding GPU hang or recovery, independently
reproduces the ordinary suspend failure. The display service is stopped and no
phoc/greetd process or DRM client remains. In t20, changing the existing runtime
policy from on back to auto triggers a normal PM attempt: `adreno_idle()` and
`a3xx_idle.part.0()` both return true, then `a3xx_pm_suspend()` returns `-EBUSY`
after one second. Generic suspend/resume never runs, runtime status stays active,
and suspended time stays zero. The earlier service stop alone produced no traced
callback; the failed attempt is identified by the explicit policy markers.

Trial t21 places a read-only, stock-binary-specific kprobe immediately before
the driver clears the VBIF request. A second on/auto attempt records:

```text
539.656022 a3xx_pm_suspend+0xb4: poll_ret=-110 request=0x7 ack=0x70007
539.656038 a3xx_pm_suspend -> adreno_runtime_suspend+0x44: ret=-16
```

The audited stock binary requests and polls `0x3f`. Its three implemented low
bits acknowledge, but waiting for all six times out. Qualcomm's downstream
source independently uses `0x7` for A306. This is a preexisting ordinary-PM bug,
separate from the recovery clock imbalance. The [A306 mask correction](https://github.com/samcday/linux/pull/6)
is under test; it leaves other A3xx masks unchanged. See the
[probe/source audit](vbif-halt.md) and [guarded helper](trace-vbif-halt.sh).

Both captures have zero missed probes and zero per-CPU overruns/dropped events;
scoped cleanup succeeds. Guest-hash-verified archives:

- t20: `0182f48f8f3b7219d201b83ab4326474a657ff6a4092c42b9641455a53c637f2`
- t21: `ab65466ed1fd3da5e38aca7966e9b15310035e006235ce85ba93d1081723d958`

The display service is restored (phoc 2361 active) before an orderly reboot.
No deliberate rendering/recovery trial ran on B10.

## B11: mask-only suspend, wakeup, and rendered-frame validation

The loaded module-note hash matches the separately built mask-only candidate
in [the VBIF audit](vbif-halt.md); recovery code is stock. All eleven payload
checksums pass. Before t22 starts, the GPU is already runtime suspended with
105,159 ms accumulated suspended time. Full snapshots show all six GPU bulk
clock enable/prepare counts at zero with hardware enable N, and genpd reports
OXILI `off-0`. The display service and phoc 1053 are alive.

After stopping the service, forced policy on/auto attempts expose a distinct
edge case: the first on resumes successfully, but auto fails ring-idle with
RPTR/WPTR `0/20`, before VBIF is checked. Three attempts return `-EBUSY`; GPU
clock counts remain at the normal active baseline, without accumulation. These
are not three successful PM cycles. No GPU hangcheck or recovery occurs. The
source defers hardware initialization until submission; a no-work resume can
leave the old software ring pointer. Further localization is in progress.

Restoring the service starts phoc 2542 and normal workload-driven PM succeeds.
Full R3 t23 with stock+`sysmem,flush` completes with verified loader/environment,
active scanout, the same compositor, and no new GPU hang. Its 1280×688 call-6260
PNG exactly matches the earlier reference SHA-256
`2d34c84ae83f9d0814880c0d062bf664572f191a93e6cb6beb02889ce8e36775`.
The GPU is suspended before and after that replay, with all six clocks back
at zero. The preserved t22 trace spans display restoration and t23: 20 generic
suspend returns, 20 generic resume returns and 20 successful A3xx suspend
returns, all zero, plus the three separately marked no-work failures above.

All 63 return-probe hits have zero misses; all per-CPU loss counters are zero
and scoped cleanup succeeds. The guest-hash-verified combined trace/PNG/retrace
archive is 25,413 bytes, SHA-256
`37c483adf089b3259be37622b3b1e845971dbdff25631a84c8713b6c2d9a1b5c`.
This validates ordinary workload-driven suspend/resume and rendering for the
mask-only change, while preserving the separate no-work-resume limitation.


B11/t24 further localizes the no-work failure. The mask-only module's
`msm_gpu_pm_resume()` stores `needs_hw_init=1` at byte offset `0xc0` in
`msm_gpu`; `msm_gpu_hw_init()` checks that byte and clears it after successful
initialization. An entry probe with `needs_hw_init=+0xc0(%x0):u8`, guarded by
the exact mask-only loaded-note hash, records 1 at 611.823055 s. The same normal
suspend attempt returns -16 at 612.823675 s, with the ring-drain diagnostic
`0/12`. This directly confirms the pending-initialization state rather than
inferring it from policy changes. Both probes have one hit/zero misses; all
CPU loss counters are zero and cleanup succeeds. Archive SHA-256:
`221359e379443373a6ed5e313e3caa611838750c63a51254d0b662f7727d5c37`.
The display service is restored before reboot. The separately reviewable
[draft ring-initialization guard](https://github.com/samcday/linux/pull/7)
keeps VBIF draining and is awaiting build/device validation.


## B12: recovery and halt-mask corrections together

The combined PR #5 + #6 module loads with verified note SHA-256
`afab4c6ef66bf0f0c3db27abc286aeef92131e97a0c73c125e2d5c3dde8a39f1`.
All eleven payload hashes pass. t25 replays stock/sysmem minimal under the
existing auto policy. It still hangs at 208.751694 s with fences 1173/1175;
recovery starts at 208.774132 s. Generic recovery suspend/resume return zero
at 208.781965/208.782081 s, followed by successful ordinary runtime suspend.
There is no stale-ring drain timeout. All six GPU clock counts are zero in
both before/after idle snapshots, and OXILI is off. The 23 recorded PM returns
are all zero, with zero missed probes/buffer loss and successful cleanup.

The 94,253-byte devcoredump has SHA-256
`e9342df1d3a48bdd8c38150a58d797b7e6ab29fc4a4e02491d51565b9ac6932e`.
It retains RBBM `0xe0684003`, fences 1173/1175, and indirect VS/FS loads of
128/256 bytes from UBO+576. Saved RPTR/WPTR are both 60, so it is not claimed
to have the same ring stall point as earlier stock captures. The archive SHA
is `963d228e434bcae030460b3108113ab02dab65c72bcd5ba775d91fe40eeeeff1`.

At t25 completion phoc 1047 still exists, but scanout is inactive. Thus this
run does not establish compositor survival. `org.gnome.ScreenSaver.SetActive(false)`
restores scanout; a transient MDP5 `04000000` error is recorded at 292.405466 s.
The following full-trace attempt t26 cannot open display `:0` and exits 1 before
loading libgallium, producing no PNG. Phoc 1047 crashes with SIGSEGV at
323.296433 s and the service starts phoc 2744. This is a failed display setup,
not a passing rendering test. Its retained retrace-only archive has SHA-256
`f9759f718dcf41c404254bc738c9d799fea571a5bb59a08940e2250f7bc2e08f`.
Retry t27 verifies active scanout and phoc 2744 before full stock+`sysmem,flush`.
It succeeds with verified library/environment, fences 1888/1888, the same
compositor and no new hang. The PNG exactly matches the reference. All six
GPU clock counts are zero before/after and runtime status is suspended.
The trace therefore remains a Mesa hang; the two kernel changes preserve
balanced recovery and restore idle PM. Rendering works after the display
service restarts, not through uninterrupted compositor survival.


## B13: initialized-ring guard, no-work cycles, and rendering

The all-three module's live note matches
`50790d5fbfce63bcba1f2620eba25f1ed4d763bed04e4fd8c19ec66523d37fbf`; all eleven
payload hashes pass. With the display service stopped and no DRM clients,
t28 performs three policy on/auto cycles without GPU submissions. Each on
resumes successfully and restores the normal active clock counts (five GPU
bulk clocks 1/1 and `gfx3d_clk_src` 3/3). Each auto returns to runtime suspended,
all six counts 0/0 with hardware enable N, and OXILI `off-0`.

The [guarded entry/return helper](trace-uninitialized-idle-all3.sh) directly
records `needs_hw_init=1` in all three suspend attempts, followed by generic
and A3xx suspend returns 0 at 203.696, 215.428 and 227.154 s. The previous
one-second ring timeouts are absent. This specifically exercises the new
skip condition; ordinary initialized submission paths are checked below.

After restoring phrog, t29 initially exits 1 before library loading because
display `:0` is not available, producing no PNG. No GPU hang or compositor
crash is recorded. A later readiness check confirms the X0/Wayland sockets,
active service and scanout, with phoc 2403. Retry t30 completes full stock+
`sysmem,flush` with verified library/environment, fences 1982/1982, the same
compositor and a byte-identical reference PNG. Its separate trace records
14 entries with `needs_hw_init=0`, and 14 successful returns each from generic
resume, generic suspend and A3xx suspend. All six clock counts return to zero.

Across t28/t30 all 73 entry/return hits have zero misses, all per-CPU loss
counters are zero, and both scoped cleanups succeed. No GPU hang/recovery or
ring-drain timeout occurs on B13. The two trace directories, failed t29
retrace, successful t30 retrace and PNG are archived together: 26,261 bytes,
SHA-256 `285d7e27b93b0cb3ab4b4d3917cd8031cb1e5e0f52d37ab92bc57f2dbd0f9939`.
The original Mesa negative is established on B12; B13 validates the additional
no-work guard and continued initialized rendering. Failed hardware-init
injection and other A3xx models are not covered.

## Diagnostic review follow-up

Review identified that interrupting a helper's separate `stop` invocation
could leave its probes registered. All three published helpers now install
stop-path EXIT/INT/TERM cleanup traps, preserve the original exit status and
mark interrupted/incomplete captures explicitly. [Local process tests](tests/test-stop-cleanup.py)
exercise normal stop, real SIGINT/SIGTERM during a blocked FIFO read, and a
cleanup failure while interrupted: all 12 cases pass. These test the stop
control flow with a mocked teardown, without touching tracefs or devices.
Bash syntax and ShellCheck also pass. Hardware captures above precede this
stop-trap robustness change; their normal scoped cleanup already succeeded.
The VBIF audit now correctly calls the five configured probes "probes," as
the two generic callbacks had no hits in the stock localization test.

## B14–B18: submit-boundary bisection and the HLSQ_FLUSH fix (Claude, 2026-09-23)

All runs: DB410c/A306, Fedora 7.3-rc3 with the **all-three** msm module (Linux
#5+#6+#7 above, note `50790d5f…`), Mesa 26.2.2 libraries under
`LD_LIBRARY_PATH`, shader cache disabled, `FD_MESA_DEBUG=sysmem` unless stated,
reduced trace `a306-minimal-pair.trace` (`MIN`) unless stated. Root fixture
`out/adreno-rob-20260923/fixture-b14/pfroot-b14.img` (export `2379873623`;
v1 `d6e0aa51…`, v2 `4f20d9e8…`, v3 `dd3aea67…` from B16), boot image
`liveboot-a3b14a.img` `ada3fbfa…`.
Every run records the mapped library path from `/proc/<pid>/maps` and the
process environment; "hang" means a new `hangcheck detected` naming eglretrace.

Libraries:

| Arm | SHA-256 | Content |
| --- | --- | --- |
| stock | `ff153c4a…` | Mesa 26.2.2, rebuilt byte-identical to Astra's stock arm |
| b14 / b14c | `c84b5379…` / `06928df7…` | stock + env-selected diagnostic knobs (`FD3_B14`); knobs off gives the stock stream on the drm-shim (`fe218cad` summary) |
| fixL | `5ca59c1b…` | stock + `CP_EVENT_WRITE HLSQ_FLUSH` before each FS indirect constant load |
| fixI | `472c3ee4…` | stock + `HLSQ_FLUSH` before every indirect constant load (any stage) |
| fixF | `e7dc0abf…` | stock + `HLSQ_FLUSH` between the VS and FS constant uploads |

Knob positions (all host-verified to add only the named packets): **S** start of
the FS-indirect draw's state, **A** every draw, **E** after each draw packet,
**F** after the VS constant uploads / before the FS ones, **V** right before the
VS indirect load, **M** right after it, **L** right before the FS indirect load.

### B14 (`791af1ee`)

| Trial | Arm / knobs | Result |
| --- | --- | --- |
| t31 | stock | **hang** (fences 1199/1202) — kernel control |
| t32 | b14, knobs off | hang — library control |
| t33 | `restore@S`: `fd3_emit_restore()` + full state re-emit before the FS-indirect draw | **pass** |
| t34–t39 | `inv@S`, `uche@S`, `dummy@S`, `presv@S`, `cf@S`, all restore packets except its register block | hang ×6 |
| t40 | `epi@E` (kernel epilogue HLSQ_FLUSH+WFI+CACHE_FLUSH_TS after every draw) | hang |
| t41 | `ib@S` (second IB2 starting at the FS-indirect draw) | hang |
| t42 | stock, full R3 keeping only clear 5785 + clip-none draw 5830 | pass |
| t43 | b14 `vscache` on that pair (VS forced to CACHE mode, VS indirect load kept) | pass |
| t44 | stock, full R3 keeping only clip-none draw 5532 + rounded draw 6119 (no clear) | **hang** |
| t45 | stock, full R3 keeping only rounded draws 6072 + 6119 (no clear) | **hang** |
| t46 | `epi@L` | **pass** |
| t47 | `inv@L` | **pass** |
| t48 | `wfi@L` | **pass** |
| t49 | `hlsq@L` | **pass** |
| t50 | `uche@L` | **pass** |
| t51 | `wfi@V` (WFI before the VS indirect load) | hang |

### B15 (`01ed4a6b`), root v2

| Trial | Arm / knobs | Result |
| --- | --- | --- |
| t52 | b14c, knobs off | hang — control |
| t53 | `hlsq@F` | hang |
| t54 | `hlsq@M` | hang |
| t55 | `vsdirect` (VS constant upload inline, FS indirect kept) | **pass** |
| t56 | fixL | **pass** |
| t57 | fixI | **pass** |
| t58 | fixF | hang |
| t59 | `wfi@F` | invalid start (display not ready, exit 1 in 1.3 s) |
| t59b | `wfi@F` | hang |

SuperTuxKart setup (Fedora `supertuxkart-1.5-4.fc45` + data, extracted under
`/opt/adreno-b14/stk/root`, `SUPERTUXKART_DATADIR` set, SDL Wayland, fresh
`HOME` per run). STK reports `OpenGL ES 3.0 Mesa 26.2.2`, renderer `FD307`.

| Run | Arm | Result |
| --- | --- | --- |
| s60 | fixI | **setup failure**: no player profile, STK waited on its first-run name screen for 900 s (54 fences, no hangcheck) |
| s61 | fixI | **setup failure**: seeded `players.xml`; STK then spent 660 s CPU-compressing textures (S3TC via libsquish, 688 KiB cached, 77 fences, no hangcheck) |
| s62 | fixI | **setup failure**: texture compression off; STK then idled in its main menu for 660 s (79 fences, no hangcheck): STK 1.5 starts `--benchmark` only together with `--no-start-screen` (`main.cpp`, `m_no_start_screen && m_benchmark`) |

Fix: seed `players.xml` (one non-guest player) and a `config.xml` generated by
STK itself with only `enable_texture_compression="false"` and
`enable_high_definition_textures="2"` changed (hashes `6179af98…`, `75c860ce…`).
All later runs pass `--no-start-screen --benchmark`. None of these three failures
is a GPU result.

| Run | Arm | Result |
| --- | --- | --- |
| s63–s69 | fixI | **setup failure**, same stall after `--no-start-screen`, internet off, sound options off, X11 instead of Wayland. A `/proc/<pid>/mem` return-address scan of the stuck main thread showed it blocked in **OpenAL's PipeWire backend** (libc futex ← libspa ← libopenal ← PipeWire): the guest's PipeWire has no sink and STK opens the audio device unless `--no-sound` is given |
| s70 | fixI | got through loading and read the Black Forest replay, then the **global OOM killer** fired (STK ~938 MB VM, 258 MB swapped; feedbackd killed) and STK exited 1. No hangcheck. Cause: texture compression had been disabled; the A5 run used compressed textures from its cache |

Validation of fixI on B15 (default GMEM unless stated):

| Run | Workload | Result |
| --- | --- | --- |
| t57, t60b, t61b | minimal pair, sysmem | **3/3 pass** |
| t62b | stock `sysmem,flush`, full R3, snapshot | reference frame `2d34c84a…` (identical to Astra's t08) |
| t63b | fixI, full R3, **sysmem**, snapshot | pass, frame `2d34c84a…` — **identical** |
| t64b | fixI, full R3, **default GMEM**, snapshot | pass, frame `2d34c84a…` — **identical** |
| g65 | fixI, #82 probe animated 12 tiles | 450/450, exit 0, no hangcheck; **10 IOMMU fault lines** 18 s in, right after an unnecessary session restart by the harness (row-sized 0x1400 iova stride) |
| g6x-patchA | Patch A, same probe (control) | 450/450, no hangcheck, **0** IOMMU faults |
| g6x-fixI | fixI, same probe, no restart beforehand | 450/450, no hangcheck, **0** IOMMU faults |
| st67 | fixI, Settings background 85 s + six panel switches | all six requests rc 0, same PID, no events (library mapping not captured — checked too early) |
| st68 | fixI, same, library checked at 30 s | `fixI/libgallium` mapped, EGL backend, six requests rc 0, same PID, 0 hangchecks, 0 IOMMU faults |

t60–t64 first attempts were invalid starts (exit 1 in <0.5 s): the OOM episode had
killed Xwayland. Kernel events from t60b on are counted by timestamp (`newk`), because
the wrapped dmesg ring made the earlier diff-based count include dropped lines; the
timestamp count confirms **zero hangchecks after uptime 6000 on B15** (all runs above).

### B16–B18 (`a69a996a`, `a49dbae0`, `e3393b1b`): SuperTuxKart, root v3, default GMEM

Root v3 (`dd3aea67…`) bakes the A5's STK seed (config with internet and sound
off, 1280×720, texture compression on; players/input) and the A5's 158 MiB
texture cache (`XDG_CACHE_HOME`). With the greeter session running, STK is
OOM-killed on this 1 GiB board even with compressed textures (s71, B16: swap
full at 208 kB free of 874 MiB, phrog 212 MiB swapped out, STK 220 MiB resident;
no hangcheck). All runs below stop the greeter session and run STK as root
straight to KMS (`SDL_VIDEODRIVER`/`SDL_VIDEO_DRIVER=kmsdrm`, GBM) with
`supertuxkart --no-start-screen --benchmark --no-sound`, no `FD_MESA_DEBUG`, and a
600 s timeout (900 s for s72).

| Run | Boot | Arm | Result |
| --- | --- | --- | --- |
| s72 | B16 | **fixI** | **complete**: 762 frames in 74 847 ms (typical 10 FPS); 0 hangchecks, 0 IOMMU faults, 0 OOM |
| s7x-patchA | B16 | Patch A (no FS push-UBO) | **SoC reset** at least 20 s in: the UART goes straight from `LOADED` to PBL/SBL1 with no kernel message |
| s7y-b2 | B17 | direct-wait (the matched rebuild of B2's upload change, not the original B2 binary) | complete: 762 frames in 73 868 ms; 0 hangchecks, 0 faults, 0 OOM |
| s7y-fixL | B17 | fixL (flush before **FS** indirect loads only) | **SoC reset**, same signature |
| s78-fixI | B18 | **fixI** (repeat, fresh boot) | **complete**: 762 frames in 74 045 ms; 0 hangchecks, 0 faults, 0 OOM |
| s79-stock | B18 | stock | **SoC reset**, same signature (last run of the session) |

Each reset left the board in U-Boot fastboot, and it was livebooted again from
there without a power cycle. fixL resetting where fixI completes twice indicates
that STK also needs the flush before the vertex-stage indirect loads; the minimal
pair only needs the fragment one (t51, t56). For scale, Sam's A5 report (also A306)
records 762 frames in 71 353 ms for B2 ([rob-followup.md](rob-followup.md)).

### Result

**Named change:** emit `CP_EVENT_WRITE HLSQ_FLUSH` immediately before every
`CP_LOAD_STATE` of constants with `SS_INDIRECT` (fixI). Indirect loads remain in
the stream (host-verified). Without it the minimal pair hangs (t31, t32, t52 and
every hanging control); with it the minimal pair passes 3/3 (t57, t60b, t61b),
the full trace renders bit-identical frames in sysmem and default GMEM (t63b,
t64b = reference `2d34c84a…`), the #82 probe completes 450/450 without faults
(g6x-fixI), Settings survives six panel switches (st68), and STK completes twice
(s72, s78) where stock, Patch A and fixL reset the SoC.

**Where the separator must sit (minimal pair):** every element placed directly
before the FS indirect load that contains an `HLSQ_FLUSH` or a `CP_WAIT_FOR_IDLE`
avoids the hang (t49 `hlsq`, t48 `wfi`; t46 `epi`, t47 `inv` and t50 `uche` each
include one of them). In the hanging stream that load (destination offset 16,
32 units) directly follows the FS direct upload (offset 0), while the flush that
stock `fd3_emit_state()` emits before all constant uploads, and the WFI from
`ring_wfi()`, precede the VS indirect load. The same events do
not help after the VS indirect load (t54), before it (t51), between the VS and FS
uploads (t53, t58, t59b), at the start of the draw (t34, t35, t39), or after
the previous draw (t40), and neither does a new IB at the draw (t41). A full
`fd3_emit_restore()` plus re-emitting all state avoids it (t33); the restore
packets without its register block do not (t39). The hang also needs the VS
indirect load in the same draw (t55: VS constants inline → pass). In every
hanging variant another draw precedes the rounded-clip draw in the same submit,
and it need not be the clear (t44, t45); no single-draw control was run in this
series. Forcing the passing clip-none VS into CACHE mode does not make it hang
(t43).

**Limitations:** one board (A306 `03000600`), Fedora 7.3-rc3 with the all-three
MSM candidate, Mesa 26.2.2 backports of the change; the Mesa-main patch is
host-verified (build, clang-format on the hunk, drm-shim diff) but was not run on
hardware. The mechanism inside HLSQ is inferred from these placements, not from
documentation. STK was run without the compositor (KMS) because of RAM.

### Mesa main patch

[mesa-main-hlsq-flush-indirect.patch](mesa-main-hlsq-flush-indirect.patch)
(SHA-256 `80522987…`) is the fixI hunk with a longer comment, committed on Mesa
main `e3f3616` (2026-09-23 08:19 UTC). It builds (freedreno gallium, x86-64,
no warnings), and clang-format 23.1.1 with freedreno's `.clang-format` leaves the
added lines unchanged. On the drm-shim (`FD_GPU_ID=307`, the reduced GTK4 draw
pair, `sysmem`), the packet summaries of main and main+patch differ only by
`CP_EVENT_WRITE HLSQ_FLUSH` directly before each `SS_INDIRECT` `ST_CONSTANTS`
load: two for the rounded-clip draw (VS `DST_OFF 16`, 16 units; FS `DST_OFF 16`,
32 units) and one for the clip-none draw (VS). The indirect loads remain.
Summaries: base `650f61b9…`/`0c24010f…`, patched `ddf3a60e…`/`33cbb944…`.
This exact commit has not run on hardware; the hardware arms are the 26.2.2
backports below.

### Files

- [fd3-hlsq-flush-indirect.patch](fd3-hlsq-flush-indirect.patch) (fixI),
  [fd3-hlsq-flush-fs-indirect.patch](fd3-hlsq-flush-fs-indirect.patch) (fixL),
  [fd3-hlsq-flush-fs-uploads.patch](fd3-hlsq-flush-fs-uploads.patch) (fixF):
  Mesa 26.2.2, `patch -p1`.
- [fd3-b14-knobs.patch](fd3-b14-knobs.patch): the `FD3_B14` diagnostic knobs
  (b14c), Mesa 26.2.2, not for upstream.
- [b14/](b14/): guest helpers `b14-trials.sh` (v3, baked into root v3) and
  `stkkms.sh`, and host tools `devregs.py` (devcoredump registers),
  `stackscan.py` (return-address scan of a stuck process) and `pixcmp.py`.
  B14/B15 defined their helpers interactively; the UART log records each
  definition as typed.

Evidence under `out/adreno-rob-20260923/` (not committed):

| File | SHA-256 |
| --- | --- |
| `hardware/uart-b14.log` (all of B14–B18, 905 064 bytes) | `8913afef…` |
| `hardware/b14/b14-t31-stock.devcore` | `11b63572…` |
| `hardware/b14/b14-boot1-logs.tar` (B14 `/run` logs) | `a633c895…` |
| `hardware/b14/b14-boot2-logs.tar` (B15 `/run` logs) | `24b17291…` |
| `hardware/b14/t64b-fixI-gmem-0000006260.png` | `2d34c84a…` |
| `fixture-b14/pfroot-b14.img` (root v3, unchanged after B18) | `dd3aea67…` |
| `fixture-b14/liveboot-a3b14a.img` | `ada3fbfa…` |

B16–B18 kept their per-run logs in `/run`, which each reset erased; their
summaries are in the UART log.

## A5 corroboration: SuperTuxKart on a second A306 (Claude, 2026-09-23)

Device: Samsung Galaxy A5U (EUR) (`a5u-a3xx-v3`), postmarketOS edge (musl), kernel
`6.12.1-msm8916` (`linux-postmarketos-qcom-msm8916` 6.12.1-r6), Phosh 0.57 (phoc
on GLES2), Alpine SuperTuxKart 1.5-r1. Its installed Mesa is the pmaports
`temp/mesa` fork (26.2.3-r1 with Patch A, the UBO-upload clamp and `23575.patch`),
so every arm here, stock included, is a per-process `LD_LIBRARY_PATH` override of
`libgallium-26.2.3.so`.

Libraries: pristine Mesa 26.2.3 (tarball SHA-512 matches the APKBUILD) plus the
patch named below, freedreno only, release (`-DMESA_DEBUG=0 -DNDEBUG -O3`), with
the options of the 2026-09-22 A5 B2 build (X11/GLX enabled, which provides the
`loader_dri3_*` exports the packaged front-ends import). All three came from one
`podman run --arch arm64` container on `alpine:edge` (image `42d86a31…`, gcc
15.2.0, meson 1.12.0, `qemu-aarch64-static` bind-mounted) running
[a5/build-alpine.sh](a5/build-alpine.sh) `<arm>` on `/work/<arm>/mesa-26.2.3`.

| Arm | Change from pristine 26.2.3 | SHA-256 |
| --- | --- | --- |
| stock | none | `d80dea93…` |
| fixI | [fd3-hlsq-flush-indirect.patch](fd3-hlsq-flush-indirect.patch) | `a9705324…` |
| fixL | [fd3-hlsq-flush-fs-indirect.patch](fd3-hlsq-flush-fs-indirect.patch) | `b980ed2d…` |
| B2 | Sam's 2026-09-22 library, reused as a control: every stage's constants copied inline (`SS_DIRECT`) | `2358e7e5…` |

Checks before any run:

- Both patches apply to 26.2.3 without offset or fuzz, and each source tree
  differs from pristine only in `fd3_emit.c`. Between the stock and fixI build
  trees, the only other differences are the line order of the generated
  `isa/encode.h` and `registers/adreno.xml.h` (identical once sorted).
- In `fd3_emit_const_bo`, fixI stores `0xc0004600` and `0x7` (`CP_EVENT_WRITE`
  `HLSQ_FLUSH`) unconditionally, directly before `0xc0013000` (`CP_LOAD_STATE`).
  fixL does so only when `v->type == 4` (fragment). Stock has no event write in
  the function.
- The phone's installed Mesa front-ends are byte-identical to the pmbootstrap
  `mesa-*-26.2.3-r1` packages, and every symbol that `libEGL`, `libGL`,
  `libGLESv2`, `libGLESv1_CM` and `dri_gbm.so` import from libgallium is defined
  by all three builds.

Procedure, identical for every run ([a5/run-stk.sh](a5/run-stk.sh); a5-s1 ran it
without the final copy of STK's CSV profiles): wake the panel (Mutter
`PowerSaveMode` 0), unlock the Phosh session through logind, then run as `user`
from SSH:

    env XDG_RUNTIME_DIR=/run/user/10000 WAYLAND_DISPLAY=wayland-0 \
        LD_LIBRARY_PATH=/home/user/a3xx/<arm> supertuxkart --benchmark

STK's configuration is unchanged from Sam's 2026-09-22 runs (720×1186 window,
texture compression and sound on). In every run `ldd` resolves libgallium to the
arm, and `/proc/<pid>/maps` of the running `supertuxkart` maps only the arm's
library. The charger was connected throughout (battery 97–98 %, charging).
`dmesg` is root-only on this image, so the kernel log was read from logbookd
(`logread`) and streamed to the host during each run; logbookd keeps nothing
across a reset.

| Run | Boot | Arm | Result |
| --- | --- | --- | --- |
| a5-s1 | `d41d93b2` (up 33 h) | **fixI** | **complete**: 763 frames in 49 744 ms (typical 15 FPS); exit 0; no kernel messages |
| a5-s2 | `d41d93b2` | **fixI** | **complete**: 763 frames in 49 857 ms; exit 0; no kernel messages |
| a5-s3 | `d41d93b2` | B2 | complete: 763 frames in 49 817 ms; exit 0; no kernel messages |
| a5-s4 | `d41d93b2` | stock | **SoC reset** 30–45 s after launch: STK's log stops at `Track: Overall scene complexity`, the line before the replay starts; no kernel message reached the host; the phone rebooted by itself (`47142ab1`) |
| a5-s5 | `47142ab1` (fresh login) | fixL | **SoC reset** 31–48 s after launch, same last STK line, no kernel message; rebooted by itself (`8094e1cd`) |

Apart from the profiler line, the fixI logs match Sam's 2026-09-22 B2 log line
for line (same shaders, warnings and scene complexity). His retained B2 log
reads 762 frames in 71 300 ms and his `nouboopt` log 121 733 ms. B2 under this
harness takes 49 817 ms, so that gap comes from the run conditions, not the
library.

**Result:** on a second A306 with a different kernel, libc and Mesa point
release, fixI completes SuperTuxKart twice while matched stock and fixL each
reset the SoC, the same outcome as B16–B18 on the DB410c. Stock and fixL ran
once each, and the A5 has produced a spontaneous oops with no GPU work before,
so one reset there is weak evidence on its own. Both resets happened at the same
point in the benchmark, which fixI and B2 passed three times on the same harness.

Evidence under `out/adreno-rob-20260923/a5-stk-20260923/` (not committed; its
`SHA256SUMS`, `916a401c…`, covers all 83 files, including the three libraries as
`.xz`, the build logs and the disassembly):

| File | SHA-256 |
| --- | --- |
| `a5-s1-fixI/phone/stdout.log` | `d4f89d2b…` |
| `a5-s2-fixI/phone/stdout.log` | `19fe9ac2…` |
| `a5-s3-B2/phone/stdout.log` | `6795aebf…` |
| `a5-s4-stock/run-follow.txt` (run log streamed to the host) | `d1b8caa2…` |
| `a5-s5-fixL/run-follow.txt` | `86ebea00…` |
| `a5-s4-stock/phone/stdout.log.partial`, identical to `a5-s5-fixL/phone/stdout.log.partial` | `ad12c117…` |
| `prior-sam-20260922/stdout.log.2` (Sam's B2 run) | `fdbab115…` |

## Piglit soak of Mesa MR !44653 on the DB410c (Claude, 2026-09-24), B26–B33

Question: with the MR (`d39138dcdcd`, `CP_EVENT_WRITE HLSQ_FLUSH` before every
indirect const load), which of the a306 CI skips, jobs and trace labels that
exist because of hangchecks can be lifted, measured against the MR's own
base (`aa1139b2e13`) as the control.

### State found at the start

- The Codex worktree that held this branch (`~/.codex/worktrees/658f`) no
  longer exists: Codex removed it when thread `01a0cb44` was archived at
  09:32 AEST. Its untracked `out/adreno-rob-20260923/` went with it:
  the B14 fixture, the handover UART tools, `CURRENT-STATE.json`, the A5
  evidence and all of the SoC-reset investigation's B19–B25 evidence
  (session `zen-cohen-104696`, which also kept its scripts in untracked
  `tools/adreno/b19/`). Nothing remained on disk to preserve. That session's
  own summary survives only in its transcript. The evidence paths quoted in
  the B14–B18 and A5 sections above now point at nothing.
- No `smoo-host` or UART holder was left behind. The board sat in U-Boot
  fastboot (`bc72e60`).
- UART: `/dev/ttyUSB2` is a clone FT232R with serial `A5069RR4`, the same
  serial as the A5 session's adapter on `ttyUSB0`, so `by-id` is ambiguous.
  Everything here used
  `/dev/serial/by-path/pci-0000:6a:00.0-usb-0:1.1:1.0-port0`. A
  `fastboot reboot` printed SBL1 and `Qualcomm-DragonBoard 410C` on it.

### Fixture

- Root: a copy of the 2026-09-21 re-provision root (`pfroot-adreno.img`,
  `d41fd968…`), grown to 12 GiB. phrog and systemd-oomd are masked. Mesa
  CI's own `debian/arm64_test-gl` image
  (`20260921-vvl-1d--20260805-spirv-llvm--f0340389--aec7a6ce…`, digest
  `c1458694…`) is unpacked under `/var/opt/ci-root` as a chroot, minus
  ANGLE, OpenCL-CTS, SKQP, deqp-gl, ci-kdl and libclc. It carries piglit
  `81f9eac8` (CI's revision and patch), deqp-runner 0.23.3, Weston 14.0.1
  and Xwayland 24.1.8.
- Kernel and initrd: the re-provision boot image (stock Fedora
  `7.3.0-0.rc3.260918g5dd1818b15d9.36.fc46`, none of Linux #5/#6/#7),
  repacked with the new `rd.smoo.root=2334208532` by
  [piglit/mkboot.sh](piglit/mkboot.sh). The smoo export id is FNV-1a over
  the block size, block count and `file:<path>`
  ([piglit/export-id.py](piglit/export-id.py), which reproduces the old
  root's `3750148101`). For `msm.num_hw_submissions=1`, a second zstd cpio
  carrying `/etc/modprobe.d/a306-ci.conf` is appended to the initrd, because
  msm loads there and the cmdline already sits at 511 bytes. The value was
  read back from `/sys/module/msm/parameters` on every boot.
- Mesa: one tree, built in CI's `debian/arm64_build` image (digest
  `eff543d2…`) under qemu-user, `debugoptimized` like the `debian-arm64` job
  (asserts on, `MESA_DEBUG=0`, so no marker WFIs), freedreno only, glvnd off,
  prefix `/install` ([piglit/build-mesa.sh](piglit/build-mesa.sh)). Main was
  built first. Then the MR's `fd3_emit.c` was swapped in, the only
  difference between the two source trees, and rebuilt incrementally. Only
  `libgallium` differs between the installs: main `bd66161d…`, MR
  `acf22d6d…`. A diagnostic third arm adds `OUT_WFI` directly before the
  MR's flush: `b002662b…`.
- Runner ([piglit/run-suite.sh](piglit/run-suite.sh)) mirrors
  `init-stage2.sh`, `weston.sh` and `deqp-runner.sh`: headless Weston with
  Xwayland, `EGL_PLATFORM=surfaceless`, the MR branch's
  `freedreno-a306-{fails,flakes}.txt`, `all-{skips,flakes,single-thread,slow-skips}.txt`
  and the suite tomls. It empties the tmpfs shader cache before each run,
  as each CI job starts empty (from B30 on). Skip variants: `ci` (as on the
  branch), `nohang` (Hangchecks block removed, used only for the
  one-at-a-time runs) and `candidate` (the proposed change below). Long
  suites ran as `--fraction K --fraction-start N` chunks, so a harness fault
  cost one chunk.

### Harness fault: lost smoo root reads (not GPU)

Four runs stopped because root-filesystem reads never completed: B27, B28,
B29 (main control) and B30 (quick-gl chunk 2). Each time `dm-0/inflight`
showed 1–17 reads stuck for up to 87 minutes. The processes sat in
`wait_on_buffer` or `folio_wait_bit_common`, the GPU was idle
(`rptr == wptr`, fences retired) and `smoo-gadget`'s ublk thread was idle.
No error appeared on either host or gadget, and fresh `O_DIRECT` reads still
worked. B28 stalled on its first test with no GPU work at all.

The other session on this host (fastboop, test-sargo) confirmed that it
never opened this gadget: its discovery matches `ff/42/03`, and ours is
`ff/53/4d`.

The likely mechanism is on our own host. `smoo-host` clamps
`--timeout-ms` to 200 ms, so it cancels and resubmits the interrupt-IN
request transfer about five times a second. A request that arrives during
a cancel is discarded by the synchronous libusb/rusb path. B31–B33 ran with
that clamp raised ([piglit/smoo-host-interrupt-timeout.patch](piglit/smoo-host-interrupt-timeout.patch),
a private build of smoo `523f726` run with `--timeout-ms 10000`). They
completed about 2¾ h of suites with no stall. That is consistent with the
hypothesis, but it doesn't prove it.

The main-arm timeouts and ~3× slowdown in B29 came from that stall together
with a shader cache left in tmpfs by the MR run before it. B33's clean main
control has neither, so B29's main numbers aren't used below.

### 1. Hangchecks skip block, one test at a time (`--jobs 1`, gpu toml)

The 14 regexes match 413 tests; 16 of them stay skipped by `.*msaa.*`.
Hangcheck counts include deqp-runner's automatic retry of each failure.

| Skip entry | main (`aa1139b2e13`) | MR |
| --- | --- | --- |
| `arb_uniform_buffer_object@rendering-array`, `-offset` | Fail, hangcheck on both attempts, each test (B27) | **Pass**, no hangcheck (B26, B32 and every full suite) |
| `gles-3.0-transform-feedback-uniform-buffer-object` | 1 hangcheck, recorded Pass (B29) | **Fail**, no hangcheck (B26, B32 and every full suite) |
| DSA `transformfeedback-bufferbase`, `-bufferrange` | Fail, 2 hangchecks each (B27, B29) | Fail, 2 hangchecks each (B26) |
| `tex-miplevel-selection` (322) | 5 of 6 sampled variants hangcheck, 6 hangchecks (B33) | hangchecks throughout; after ~375 of 386 tests the GPU never recovered (`a3xx_idle: timeout waiting for GPU to idle`, then about 1 hangcheck/s with fences stuck, test and Weston in `dma_fence_default_wait`), so sysrq reboot (B26) |
| `arb_point_sprite-interactions 1.0` | Fail, no hang | Fail, no hang |
| drawarrays 100000 `gl_lines` ×2, `fbo-stencil-gl_stencil_index8-clear`, `srgb_conformance`, `dlist-fdo3129-01` | Pass | Pass |
| `arb_framebuffer_srgb` blit renderbuffer linear / texture srgb (64 non-msaa) | 64 Pass | 64 Pass |

The xfb-UBO test explains the difference. Pass *i* writes `out_pass = i`
through transform feedback, then reads the same buffer back as a UBO, which
a3xx pushes into constants with an indirect `CP_LOAD_STATE`. With the MR,
passes 1 and 2 read stale values: expected black at (0,0), observed
(0,1,1), 2/2. On main the GPU hangs and the test then reports Pass,
presumably because the draws lost in recovery leave the target black. The WFI arm (WFI immediately before the
flush) gives the same results as the MR on this test and on
`rendering-array` (B32, `b32-x1-*`). A WFI at that point therefore neither
fixes the stale read nor changes the MR's outcome, which bears on the
`fd_reset_wfi()` question.

### 2. Full suites with the MR and the candidate skips

| Job (toml, jobs, cmdline) | Boot | Results | Time | Hangchecks (all recovered) | Not in the MR branch's baseline |
| --- | --- | --- | --- | --- | --- |
| `a306-piglit` (gpu, 8, `PIGLIT_NO_WINDOW`) | B29 | 37 526 | 36:11 | 2: `glsl-1.40 tf-no-position` (baseline Fail), `fs-bitfieldInsert` | xfb-UBO Fail, point_sprite Fail, `rasterpos` Crash, 4× `mesa_shader_integer_functions` bitfield{Extract,Insert} Fail, 2 Timeouts (singles in B33: Pass and Skip, as on main) |
| `a306-piglit-shader` (quick_shader, 6) | B32 | 18 024 | 17:13 | 1: `fs-bitfieldInsert` | the 4 bitfield Fails |
| quick_gl, 3, default `num_hw_submissions` (8) | B32 | 19 502 | 17:44 | 1: `tf-no-position` | xfb-UBO, point_sprite, `rasterpos` |
| **`.a306-piglit-gl` as disabled** (quick_gl, 3, `msm.num_hw_submissions=1`) | B30 + B31 | 19 502 | 28:13 | 4: `tf-no-position`, `ext_transform_feedback order elements lines`, `order arrays triangles` | xfb-UBO, point_sprite, `rasterpos`; Flakes: both `order` tests, `vbo-subdata-many drawrangeelements` |

Every suite ran to completion. No SoC reset, no unrecovered hang and no
IOMMU fault occurred in any MR suite. Each unskipped test except xfb-UBO and
point_sprite passes in all three quick_gl-containing configurations.

Main control (B33, CI skips, gpu, 8 jobs, 37 524 results, 41:49) has 3
hangchecks: `tf-no-position` once and `fs-bitfieldInsert` twice. Its
unexpected results are `rasterpos` Crash and the same 4 bitfield Fails.
Against the MR's gpu run, the only differing tests are the unskipped ones,
plus the two MR timeouts and an `fbo-colormask-formats` flake. The
timeouts were Pass/Skip as singles, and the flake recurs on both arms. So
`rasterpos`, the bitfield Fails, `tf-no-position` and the
`fs-bitfieldInsert` hang are pre-existing on main. Each `submit failed: -12`
dumps about 33 000 `msm_dump_submit` lines to the 115 200 baud console.
That stretches gpu chunks 2 and 4 on both arms (main's chunk 2 had two
bursts, the MR's one).

### 3. Hang- and crash-labelled traces (`run-traces.sh`, eglretrace headless, `-j1`)

| Trace | CI label | main (B33) | MR (B32) |
| --- | --- | --- | --- |
| humus/VolumetricFogging2 | hang | pass, `aec880ce…` = CI's a306 checksum, no hangcheck | same |
| gputest/pixmark-volplosion | hang | completes, `9ca37688…` (32×32 frame), no hangcheck | same |
| humus/Portals | crash | completes, no assertion, `01d97a82…` (32×32 frame) | same |
| 0ad/0ad-v2 | hang | 6 hangchecks, checksum mismatch | 5 hangchecks, mismatch |
| gputest/furmark | hang | 2 hangchecks, `6099155d…` ≠ `751e0e78…` | same |
| gputest/triangle (control) | — | pass | pass |

The MR doesn't change any trace. Three labels are stale on this kernel on
both arms, and 0ad and furmark still hang with it.

### Proposed CI change (Mesa, not pushed)

Branch `claude/a306-ci-hlsq-expectations` in the worktree
`~/tmp/a306-piglit-20260924/mesa-ci` of `~/src/mesa`, on top of
`d39138dcdcd`. It is copied here as
[mesa-ci-a306-expectations.patch](mesa-ci-a306-expectations.patch). Both
commit messages are placeholders for Sam.

1. `bf5c94b02c1`: MR-attributable.
   - Drops 11 entries from the Hangchecks skip block.
   - Keeps `tex-miplevel-selection` and the two DSA transform-feedback entries.
   - Adds Fail entries for xfb-UBO and `arb_point_sprite-interactions 1.0`.
     The point_sprite entry was dropped as shadowed in `8856291a8bc`.
   - Renames `.a306-piglit-gl` to `a306-piglit-gl`, keeping
     `msm.num_hw_submissions=1` with a note that quick_gl also completed
     without it.
2. `96a9d97260f`: pre-existing on main and optional. Records `rasterpos`
   Crash and the four bitfield Fails, plus flakes for `vbo-subdata-many
   drawrangeelements`, `fbo-colormask-formats` and the two
   `ext_transform_feedback order` tests. The `order` tests were seen
   hangchecking only with `num_hw_submissions=1` on the MR arm; that
   configuration was not run on main.

`traces-freedreno.yml` is left alone, because the MR changes no trace
outcome.

Limits:

- One DB410c, a stock 7.3-rc3 kernel rather than CI's `v7.1-mesa-3b43`, and
  Fedora's userspace outside the chroot.
- Each configuration ran once.
- The main arm was not run through quick_gl or quick_shader separately.
  gpu = quick_gl + quick_shader with `PIGLIT_NO_WINDOW`.

### Files

Raw evidence stays outside git under `~/tmp/a306-piglit-20260924/evidence/`.
Its `SHA256SUMS` (`4b15fbee…`) covers all 395 files. The root image ended at
`1c160483…` after the runner and WFI-arm bakes; it started at `b06e519a…`.

| File | SHA-256 |
| --- | --- |
| `b26/b26-h1-mr-ubo.tar.zst` | `0a10fd1c…` |
| `b26/b26-h2-mr-misc.tar.zst` | `8787614a…` |
| `b26/b26-h3-mr-srgb-mip.tar.zst` | `b6aed992…` |
| `b27/b27-h1-main-ubo-partial.tar.xz` | `0468cecb…` |
| `b27/b27-h2-main-misc.tar.zst` | `025e11d6…` |
| `b27/b27-h3-main-srgb.tar.zst` | `a4eac608…` |
| `b27/b27-h3-mr-srgb.tar.zst` | `c2929415…` |
| `b29/b29-h1-main-rest.tar.zst` | `edec370e…` |
| `b29/b29-p1-mr-gpu-f1of4.tar.zst` | `dc0e9777…` |
| `b29/b29-p1-mr-gpu-f2of4.tar.zst` | `3c942a15…` |
| `b29/b29-p1-mr-gpu-f3of4.tar.zst` | `4349fab6…` |
| `b29/b29-p1-mr-gpu-f4of4.tar.zst` | `d7510c76…` |
| `b30/b30-p5-mr-qgl-hw1-f1of2.tar.zst` | `e2bbc2b9…` |
| `b31/b31-p5-mr-qgl-hw1-f2of2.tar.zst` | `28154023…` |
| `b32/b32-p3-mr-qsh-f1of2.tar.zst` | `c4e5674a…` |
| `b32/b32-p3-mr-qsh-f2of2.tar.zst` | `591bdf18…` |
| `b32/b32-p4-mr-qgl-f1of2.tar.zst` | `859af0f5…` |
| `b32/b32-p4-mr-qgl-f2of2.tar.zst` | `34546378…` |
| `b32/b32-t1-mr/results.json.bz2` | `e6514940…` |
| `b32/b32-x1-mrwfi-xfbubo.tar.zst` | `fc95cdb5…` |
| `b32/b32-x1-mr-xfbubo.tar.zst` | `b0221d66…` |
| `b33/b33-h4-main-mip.tar.zst` | `d37035f6…` |
| `b33/b33-p2-main-gpu-f1of4.tar.zst` | `cbcbee4e…` |
| `b33/b33-p2-main-gpu-f2of4.tar.zst` | `bd51183a…` |
| `b33/b33-p2-main-gpu-f3of4.tar.zst` | `cd3abd47…` |
| `b33/b33-p2-main-gpu-f4of4.tar.zst` | `179d7ab9…` |
| `b33/b33-r1-mr-timeouts.tar.zst` | `f0bd1a35…` |
| `b33/b33-t1-main/results.json.bz2` | `383c942b…` |
| `smoo-host.log` | `9ac3e5f7…` |
| `uart-b26-b33.log.xz` | `fcd88dc7…` |

### B34 addendum: the bitfield failures are a main regression from `23007dcb077` (2026-09-25)

Correction to "pre-existing on main" above. Mesa's nightly `a306-piglit` and
`a306-piglit-shader` run on a DB410c in Collabora's LAVA farm (kernel
`7.1.2-g3b43d6b4a133`). They passed on 2026-09-21 and 09-22
(`6351df29e2d`) and have failed since 09-23 (`18f439ed5a2`, `ecf5201fb90`).
Their `failures.csv` lists exactly the four
`mesa_shader_integer_functions` bitfield{Extract,Insert} tests seen here, and
their logs show the `fs-bitfieldInsert` hangcheck from 09-23 on.
!44199 ("ir3: pile of opts and some new opcodes") merged between those
nightlies. Its `23007dcb077` sets `.has_bfm = true` in `ir3_base_options`
for every generation, lowering `nir_op_bfm` to `MGEN.B`.

No Mesa issue or MR mentioned the regression as of 2026-09-25 (searched
issues and MRs from 2026-09-15 on for bitfield/a306/a3xx/mgen/bfm, the
freedreno, ir3 and CI labels, and !44199's discussion).

B34, one boot, `--jobs 1`, `-t mesa_shader_integer_functions` (60 results):

| Arm | libgallium | Result |
| --- | --- | --- |
| MR (`acf22d6d…`) | as above | 52 Pass, 4 ExpectedFail, **4 Fail** (fs/vs bitfieldExtract/Insert), 1 hangcheck (`fs-bitfieldInsert`) |
| MR + `git revert 23007dcb077` | `b26b6b27…` | **56 Pass**, 4 ExpectedFail, 0 hangchecks |

Only those four tests differ. `rasterpos` Crash does not appear in CI's
complete 09-23 results, so it is specific to this lab setup. The optional
second Mesa commit (`96a9d97260f`) should therefore not be sent: it would
paper over a regression that CI already reports.

| File | SHA-256 |
| --- | --- |
| `b34/b34-bf-mr.tar.zst` | `984a0c87…` |
| `b34/b34-bf-mrrev.tar.zst` | `d43a5285…` |
| `uart-b34.log.full.xz` (UART log B26–B34) | `499e9673…` |

Evidence `SHA256SUMS` is now `b6c41cfb…` (418 files).

### B35: root cause of the a3xx bitfield regression and a fix candidate (2026-09-25)

Follow-up to the B34 addendum. Goal: explain why `23007dcb077` breaks
A306 and produce the narrowest fix. Hardware validation is still pending
(see "Not done").

**Research** (five independent read-only lenses with adversarial
verification of every claim; raw journal and summary under
`evidence/b35-research/`, hashes below):

- The four tests fail at ordinary operands (bits=1/offset=0, 4/0, 4/16),
  and pass at bits=0 and bits=32. An emulator of the lowered NIR
  (`bitfieldInsert` → `bfm(bits, offset)`; `ubitfield_extract` →
  `bfm(bits, 0)`; `ibitfield_extract` uses no `bfm`, which is why only
  the unsigned Extract probes fail) tried 20 candidate MGEN.B semantics.
  No deterministic semantic (edge-case masking, saturation, swapped
  operands, off-by-one) reproduces the observed probes; the VS results fit
  only "mask has none of the expected bits" (0 or stale). The FS bits=0
  draws fail on only some pixels, and the first failing pixel moves
  between runs ((16,0), (24,0), (64,0) here; (96,0) and (0,0) in Mesa's
  LAVA logs) although every input is a uniform. So MGEN.B's result on
  A306 is not a function of its operands.
- The encoding is the normal cat2 two-source form (`47500002_0000101a`,
  `mgen.b r0.z, c6.z, r0.x`, from Mesa's own a306 nightly crash dump),
  the same shape as the neighbouring `shl.b`/`and.b`. `mgen.b` (cat2
  opcode 58) has been in the ir3 opcode table since the 2013 a3xx import
  (`2855f3f7bcd`) but nothing in ir3 emitted it before `23007dcb077`; the
  ISA XML has no gen gate and `tests/disasm.c` has no vectors for it.
- The hang: in 14 logged runs (8 lab, 6 LAVA) the hangcheck always
  coincides with the first draw of `fs-bitfieldInsert` (bits=0,
  offset=32, signed). That FS has one branch that depends on the MGEN.B
  result, compiled as uniform (no `(jp)`); a per-fibre garbage mask makes
  it diverge. `fs-bitfieldExtract` has no branch on the mask and never
  hangs; the VS runs one wave and only fails. Mechanism inferred, not
  probed.
- CI boundary (nightly pipeline 1752008 at `18f439ed5a2`, which contains
  `23007dcb077`): a306-piglit 110916889 and a306-piglit-shader 110916890
  fail exactly the four tests, `failures.csv` byte-identical to B34;
  a530-piglit-full 110916895/110916896 pass all fs/vs/const bitfield
  tests; a618-piglit-full 110916900 passes the fs variants (vs skipped by
  the fraction); a750-gl-cl 110916915 passes all 128 KHR
  bitfieldExtract/Insert cases (a702 is CHIP.A6XX, so it is gen-6
  evidence). The MR's own pipeline 1751912 never ran a306 piglit (manual
  job). No a4xx CI job exists; the 2021 `freedreno-a420-*.txt` files list
  none of the bitcount/bitfieldReverse failures a306 carries for the
  related cat2 bit ops.
- `5c221a4a858` (GETBIT) does not reach a3xx: `has_bit_test` is set only
  in the `gen >= 6` block (`ir3_compiler.c:392`).
- Achievable target on a306 is 56 Pass + 4 ExpectedFail: fs/vs-bitcount
  and fs/vs-bitfieldreverse are long-standing expected failures
  (`freedreno-a306-fails.txt:392-395`).

**Fix candidate:** worktree `~/tmp/a306-piglit-20260924/mesa-fix`, branch
`claude/a3xx-bfm-regression`, commit `ebd188bcd32` on main `e443fef0f24`
(PLACEHOLDER message, unpushed; copy in
[mesa-a3xx-has-bfm-gate.patch](mesa-a3xx-has-bfm-gate.patch)). It drops
`.has_bfm = true` from `ir3_base_options` and sets
`compiler->nir_options.has_bfm = compiler->gen >= 4` next to the other
per-device overrides. Only a3xx changes; a4xx stays as on main, which the
commit body says is unverified. `gen >= 5` is the alternative if a4xx
should be treated as unknown-bad.

**Host codegen check** (no hardware; freedreno noop drm-shim, native
x86 builds of `e443fef0f24` and of the fix in Mesa CI's `x86_64_build`
image, shader-db `run` with `IR3_SHADER_DEBUG=disasm`;
[piglit/build-x86.sh](piglit/build-x86.sh),
[piglit/disasm-x86.sh](piglit/disasm-x86.sh),
[piglit/sdb-prep.py](piglit/sdb-prep.py),
[piglit/asm-extract.py](piglit/asm-extract.py)). Inputs: the four piglit
tests plus three probe shaders from
[piglit/probe/gen-bfm-probe.py](piglit/probe/gen-bfm-probe.py).

| GPU | base → fix | `mgen.b` count base → fix |
| --- | --- | --- |
| FD307 | only the bfm-bearing shaders change (e.g. vs-bitfieldInsert VS 122 → 128 instructions); every other shader identical | 8 → 0 |
| FD420 | all shaders byte-identical | 8 → 8 |
| FD530 | all shaders byte-identical | 8 → 8 |
| FD630 | all shaders byte-identical | 8 → 8 |

**Builds** (arm64, CI `arm64_build` image, [piglit/build-fix.sh](piglit/build-fix.sh)):
`fixbase` = main `e443fef0f24`, libgallium `33beaf3e…`; `fix` =
`ebd188bcd32`, libgallium `81615894…`. Both baked into the fixture with
[piglit/bake.sh](piglit/bake.sh) together with the probe tests and
[piglit/probe/run-probe.sh](piglit/probe/run-probe.sh) (root image now
`2c927a35…`).

**Not done (hardware):** the DB410c did not boot this session.

- UART identity: the host rebooted at 09:55 and the four FT232R clones
  renumbered. Yesterday's DB410c path `pci-0000:6a:00.0-usb-0:1.1` is
  `ttyUSB0` today, on the same controller as the board's fastboot port
  (`usb:13-1.2`); `ttyUSB2` today is `pci-0000:67:00.0-usb-0:2.4`
  (serial A50285BI) and received nothing during two board reboots. The
  session's brief allows only `ttyUSB2`, so no console was available. An
  earlier note in this session that ttyUSB2 had been "confirmed" was
  wrong: the SBL1 banner it matched was B34's old log content.
- `fastboot -s bc72e60 boot` of the 44 MB image stalled at "Sending" on
  three attempts (including after a `fastboot reboot`), while `getvar`
  and `reboot` work. Cause not found.
- The board is in U-Boot fastboot (`bc72e60`); smoo-host and the
  console logger are stopped; no other device was touched.

Planned once the console is confirmed: B35 with `run-probe.sh` on
`fixbase` (MGEN.B value sweep and the four tests with disassembly), the
shader-override probes in `payload/a306/probe/ovr/` if the root cause
wording needs them, then `run-suite.sh fix a306-piglit 1 candidate
-t mesa_shader_integer_functions` (expect 56 Pass + 4 ExpectedFail, no
hangcheck) and a full quick_shader against the B32 baseline.

| File | SHA-256 |
| --- | --- |
| `evidence/SHA256SUMS` (477 files) | `08ba23e8…` |
| `evidence/b35-research/research-journal.jsonl` | `73b3b475…` |
| `evidence/b35-research/research-summary.txt` | `8e793dc7…` |
| `evidence/b35-research/host-disasm/SHA256SUMS` (56 dumps) | `6a117474…` |
| `evidence/b35-research/mesa-a3xx-has-bfm-gate.patch` | `e7d11fc5…` |
| `out/fixbase` libgallium | `33beaf3e…` |
| `out/fix` libgallium | `81615894…` |
| `out/x86-fixbase` libgallium | `796f66f8…` |
| `out/x86-fix` libgallium | `cb9732d4…` |

### B35 results: MGEN.B never writes its destination on A306; the fix validates (2026-09-25 afternoon)

Sam confirmed the console had moved to `ttyUSB0` and reset the board;
the fastboot download then completed in 1.5 s. One boot (B35, stock
7.3-rc3 kernel, `num_hw_submissions=8`), arms `fixbase` = main
`e443fef0f24` (libgallium `33beaf3e…`) and `fix` = `32d69a37311`
(`81615894…`, the has_bfm gate; commit amended with the results below,
still unpushed).

**MGEN.B value sweep** ([piglit/probe/gen-bfm-probe.py](piglit/probe/gen-bfm-probe.py):
`(1u << bits) - 1u` and `((1u << bits) - 1u) << offset` with uniform
operands, result written as four unorm8 bytes, each case in its own
4 px column; on-board disassembly shows `mgen.b r0.x, c0.x, 0` and
`mgen.b r0.x, c0.x, r0.x` respectively):

| Arm | bfm-mask (23 cases) | bfm-shift (25 cases) | shl-ref (23 cases, `1u << bits`) |
| --- | --- | --- | --- |
| main | 18 wrong: every case with a nonzero NIR mask read **0**, except the first draw (bits=0) which read **`0x3f7fe1a7`**, stale register contents; the 5 cases whose mask is 0 anyway (bits 32/64/96/128/256) matched | 18 wrong, all read **0**; the 7 zero-mask cases matched | all 23 match |
| fix | all 23 match | all 25 match | all 23 match |

So on A306 `mgen.b` leaves its destination unwritten (or writes 0); it is
not a different mask formula and not an operand-order or edge-case
difference.

**Hang mechanism** (`fs-bitfieldInsert` on main, IR3_SHADER_OVERRIDE_PATH
variants of the piglit FS `b68051388b85…`, 3-4 runs each,
[piglit/probe/run-probe.sh](piglit/probe/run-probe.sh) `--override`):

| Variant | hangchecks | failing probes per run |
| --- | --- | --- |
| unmodified | **4/4** (one per run) | 6, 6, 6, 6 |
| A: `mov r0.z, 0x000f0000` before the `mgen.b` (sentinel = the bits=4/offset=16 mask) | 0/3 | 5, 4, 4 |
| B: `mov r0.z, 0` before the `mgen.b` | 0/3 | 4, 4, 4 |
| C: `(jp)` added at the mask-dependent branch targets, `mgen.b` untouched | 0/3 | 6, 6, 6 |
| VS variant A on `vs-bitfieldInsert` | 0/2 | 5, 4 |

A deterministic destination (A, B) or a reconvergence point (C) removes
the hang; only removing MGEN.B (the fix) removes the failures. B
reproduces the vertex-shader failure count exactly, consistent with a
zero mask. The earlier B35 probe pass ran `fs-bitfieldInsert` once
without a hangcheck, so the hang is frequent, not certain.

**Fix validation on the board:**

| Run | Result |
| --- | --- |
| `run-suite.sh fix a306-piglit 1 candidate -t mesa_shader_integer_functions` (60 tests) | **56 Pass, 4 ExpectedFail** (fs/vs-bitcount, fs/vs-bitfieldreverse, long-standing), rc=0, 0 hangchecks |
| `run-suite.sh fix a306-piglit-quick-shader 6 candidate` (18 024 results, 17:12) | 5821 Pass, 40 ExpectedFail, 2 Warn, 12 161 Skip, rc=0, 0 hangchecks; against the B32 MR baseline exactly 4 tests differ: the four bitfield tests, Fail → Pass |
| the four tests + probes on `fix` with disassembly | all pass, 0 `mgen.b` in the output |
| the four tests on main | fail with 3/2/4/5 failing probes, as in B34 and in Mesa's nightlies |

Boot count this session: 1 (plus 3 failed fastboot downloads before the
reset). Hangchecks: 4, all in the unmodified `fs-bitfieldInsert` runs
on main, all recovered. No SoC reset. The board was returned to U-Boot
fastboot; smoo-host and the console logger are stopped.

Root image after this session: `2c927a35…` (unchanged by the board;
guest writes stay in RAM).

| File | SHA-256 |
| --- | --- |
| `evidence/SHA256SUMS` (554 files) | `73af1594…` |
| `b35/b35-bf-fix.tar.zst` | `d29ce78b…` |
| `b35/b35-qsh-fix.tar.zst` | `dc1921ae…` |
| `b35/b35-probe-fixbase.tar.zst` (first probe pass, old test shape) | `c93cd8d9…` |
| `b35/b35-probe-fix.tar.zst` | `6c62cfa5…` |
| `b35/b35-probe2-fixbase.tar.zst` (sweep) | `9ead1321…` |
| `b35/b35-probe2-fix.tar.zst` | `9b08843c…` |
| `b35/b35-ovr-plain.tar.zst` | `cdae2276…` |
| `b35/b35-ovr-A-sentinel.tar.zst` | `298fbc0c…` |
| `b35/b35-ovr-B-zero.tar.zst` | `101a667a…` |
| `b35/b35-ovr-C-jp.tar.zst` | `f8f6da7d…` |
| `b35/b35-ovr-VS-A.tar.zst` | `09166a75…` |
| `uart-b35.log.full.xz` | `4994bbff…` |
| `mesa-a3xx-has-bfm-gate.patch` (`32d69a37311`) | `0225e025…` |
