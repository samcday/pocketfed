[Service]
# Bounded, failure-ignored cooperative handoff before Plymouth opens DRM.
# The daemon replies only after CLOSEFB and drop-master, so normal boot
# proceeds even when Premouth is absent or already finished.
ExecStartPre=-/usr/libexec/premouth --yield --timeout 1 --socket /run/premouth/handoff.sock
