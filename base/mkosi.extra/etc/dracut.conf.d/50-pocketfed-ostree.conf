# Generated PocketFed OSTree images need the OSTree initrd module.
add_dracutmodules+=" ostree "

# Android userdata artifacts are ext4 even when dracut runs in hostonly mode
# inside a non-ext4 build environment.
force_drivers+=" ext4 "
