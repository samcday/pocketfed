# Generated PocketFed images boot as OSTree deployments.
add_dracutmodules+=" ostree "

# The aboot userdata artifact is ext4 even when dracut runs in hostonly mode
# inside a non-ext4 build environment.
force_drivers+=" ext4 "
