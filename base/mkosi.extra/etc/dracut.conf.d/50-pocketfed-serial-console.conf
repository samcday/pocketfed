# The initrd's udevd must mark the serial console db_persist before
# switch-root, so ship the rule in every PocketFed initramfs.
install_items+=" /usr/lib/udev/rules.d/90-pocketfed-serial-console.rules "
