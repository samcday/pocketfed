# Touchscreen setup runs in the greeter after sysinit. Keep firstboot's
# credential import, including fastboop personalization, without console prompts.
[Service]
ExecStart=
ExecStart=/usr/bin/systemd-firstboot
StandardInput=null
StandardOutput=journal
StandardError=journal
TTYReset=no
