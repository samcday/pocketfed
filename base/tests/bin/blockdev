#!/bin/sh
set -eu

if [ "${POCKETFED_ALLOW_REGULAR_BOOT_PARTITION:-0}" = 1 ] &&
    [ "${1:-}" = --getsize64 ] && [ "$#" -eq 2 ]
then
    stat -c %s "$2"
    exit 0
fi

# Policy fixtures must never reach a real block-device operation.
exit 99
